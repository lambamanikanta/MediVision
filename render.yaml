# Render Deployment Configuration

services:
  api:
    type: docker
    docker:
      image: your-docker-image
      build_command: ''
      run_command: ''
    env:
      NODE_ENV: production
    plan: starter
    instances: 1
    auto_deploy: true
